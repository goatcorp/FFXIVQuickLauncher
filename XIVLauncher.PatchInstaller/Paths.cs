using System;
using System.IO;

namespace XIVLauncher.PatchInstaller
{
    public class Paths
    {
        public static string XIVLauncherPath = Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData), "XIVLauncher");
    }
}
